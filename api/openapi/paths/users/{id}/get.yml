description:
  Get a user by ID. `me` can also be used. Right now it will only return the
  current user itself.
summary: Get a user by ID.
operationId: getUserById
security:
  - bearerAuth: []
tags:
  - users
responses:
  "200":
    description: Successfully retrieved the user
    content:
      application/json:
        schema:
          $ref: "../../../components/schemas/_index.yml#/User"
        examples:
          example-1:
            $ref: "../../../components/examples/_index.yml#/UserExample"
  "401":
    description: Unauthorized access - User must be logged in to make the request
    content:
      application/json:
        schema:
          $ref: "../../../components/schemas/_index.yml#/ErrorResponse"
        examples:
          example-1:
            value:
              message: "user not logged in"
  "500":
    description: Something wrong happened

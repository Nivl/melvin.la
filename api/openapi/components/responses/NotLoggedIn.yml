description: The user is not logged in
content:
  application/json:
    schema:
      $ref: "../schemas/ErrorResponse.yml"
    examples:
      example-1:
        value:
          message: "user not authenticated"

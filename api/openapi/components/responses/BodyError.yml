description: The body contains missing or invalid data
content:
  application/json:
    schema:
      $ref: "../schemas/ErrorResponse.yml"
    examples:
      example-1:
        value:
          message: "name is required"
          field: "name"

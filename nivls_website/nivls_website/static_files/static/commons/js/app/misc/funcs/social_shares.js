/*jslint browser:true */
/*global $, gapi, twttr, FB */

function reloadShareButtons() {
    'use strict';

    // var $twttr_button = $('.twitter-share-button'),
    //     $fb_button = $('.fb-like');

    // if (typeof gapi !== 'undefined') {
    //     gapi.plusone.go();
    // }

    // if (typeof twttr !== 'undefined' && typeof twttr.widgets !== 'undefined') {
    //     if ($twttr_button.length > 0) {
    //         $twttr_button.attr('data-url', document.location.href);
    //         $twttr_button.attr('data-text', $('title').text());
    //         twttr.widgets.load();
    //     }
    // }

    // if ($fb_button.length > 0) {
    //     $fb_button.attr('data-url', document.location.href);

    //     FB.XFBML.parse(document);
    // }
}

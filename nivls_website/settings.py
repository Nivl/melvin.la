# -*- coding: utf-8 -*-
from settings_global import *

SITE_ID = 1

MIDDLEWARE_CLASSES = MIDDLEWARE_CLASSES + (

    )

INSTALLED_APPS = INSTALLED_APPS + (
    'commons',
    'about',
    'lab',
    'blog',
    'seo'
    )

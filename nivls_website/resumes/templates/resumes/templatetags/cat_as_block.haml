- load markup

- for content in cat.content_set.all
    .element
        - if content.key
            %h4<
                {{ content.key|safe }}
            .content
                {{ content.value|markdown:"def_list" }}
